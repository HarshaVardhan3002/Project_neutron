/**
 * @fileoverview Barrel file for exporting all shared TypeScript types.
 * This allows for cleaner imports in other parts of the application.
 * For example: `import type { User, Post } from '@/lib/types';`
 */

export * from './admin';
export * from './lms';
